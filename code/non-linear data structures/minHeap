#include <iostream>
using namespace std;

class MinHeap
{
    public:
        int* harr; //pointer pointing to the array of min heap
        int capacity; //maximum possible size of min heap
        int heapSize; //current no of elements in min heap

        MinHeap(int cap)
        {
            capacity = cap;
            heapSize = 0;
        }
};
